info:
  name: disconnect stripe account
  type: http
  seq: 1

http:
  method: DELETE
  url: http://localhost:3000/api/provider/auth/disconnect-stripe-id
  body:
    type: json
    data: |-
      {
        "email":"affan.farooqsbf@gmail.com"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This request retrieves a list of users from the JSONPlaceholder API.
